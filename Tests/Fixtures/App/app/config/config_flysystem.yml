imports:
    - { resource: config.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.filesystem

srio_rest_upload:
    storages:
        default:
            type: flysystem
            filesystem: oneup_flysystem.test_filesystem
imports:
    - { resource: config.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.filesystem

srio_rest_upload:
    storages:
        default:
            type: gaufrette
            filesystem: gaufrette.test_filesystem